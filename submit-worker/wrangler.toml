name = "hdt-lobbymmr-submit"
main = "src/index.js"
compatibility_date = "2024-09-01"

[vars]
GH_OWNER = "zakarulcodes"
GH_REPO = "hdt-lobbymmr-streamers"

# Secrets are NOT stored here — set them with `wrangler secret put`:
#   wrangler secret put TWITCH_CLIENT_ID
#   wrangler secret put TWITCH_CLIENT_SECRET
#   wrangler secret put GITHUB_TOKEN        # fine-grained PAT, Contents: write on this repo
#   wrangler secret put SESSION_SECRET      # any long random string
